#Going to try to modulize the game after

import pygame 

class Pipe:
	def __init__(self,location,XPos,YPos) 
		self.location = location 
		self.XPos = XPos
		self.YPos = YPos
			
	def move() 
		self.XPos = self.XPos - 1; 

		